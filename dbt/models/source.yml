version: 2

sources:
  - name: tests
    description: Connection to BigQuery
    tables:
      - name: persons
        description: >
          One record per order. Includes cancelled and deleted orders.
        columns:
          - name: name
            description: Person's First Name
            tests:
              - unique
              - not_null
